# Docker assets

The development services are orchestrated by the root `docker-compose.yml`. Service-specific build instructions live beside each service in `frontend/Dockerfile` and `backend/Dockerfile`.
